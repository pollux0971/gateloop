# failure-triage — memory
AVOID: fixing the symptom instead of the root cause.
AVOID: widening scope or deleting tests to force a pass — escalate if the fix needs more scope.
AVOID: an avoid line over 40 words; only `avoid` is injected into agent context.

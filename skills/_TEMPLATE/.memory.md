# .memory.md — per-skill memory (append-only, compact)

Compact warnings and lessons for THIS skill only. One short block per entry.
Prefer distilled AVOID-style warnings over narration; do not let this grow
unbounded — consolidate duplicates. Surfaced alongside SKILL.md when the skill
is loaded.

Format:
## YYYY-MM-DD — signature
AVOID: <one-line failure mode>. DO: <one-line corrective>.

# program-spec-assembly — memory

AVOID: shipping a spec where acceptance lists a behavior the test file never asserts — behavior drift silently breaks machine-checkability.
AVOID: letting the implementer also be the test_author — acceptance-test integrity is lost.
AVOID: a documented stub with no registry entry — the stub-registry meta test will fail the build.

# acceptance-test-scaffold — memory

AVOID: trivially-passing bodies (`expect(true).toBe(true)`) — a green skeleton hides missing coverage.
AVOID: adding "nice to have" it() cases not in behaviors_must_pass — the behavior set is the contract.
AVOID: writing the skeleton as the implementer — record a distinct test_author (acceptance-test integrity).

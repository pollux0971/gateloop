# delta-spec-authoring — memory
AVOID: write_set paths that overlap public_api_frozen — this is the most common failure.
AVOID: empty affected_files list; every delta must name what it changes.
AVOID: vague change_summary under 20 characters.

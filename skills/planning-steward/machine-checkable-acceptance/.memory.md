# .memory.md — machine-checkable-acceptance
AVOID: prose acceptance ("tests pass", "works"). A shallow test can satisfy prose.
AVOID: behavior ids with spaces/Capitalization — they must equal the vitest it() name.
LESSON: list the *.test.ts in files_must_exist so acceptance is provably test-locked.

# .memory.md — behavior-test-derivation
AVOID: only happy-path behaviors. Always add negative + boundary; add security for any
component touching paths/commands/secrets/permissions.
LESSON: one *_is_not_implemented behavior per documented stub locks the stub.
LESSON: the planned behavior id is the contract — the implementer must not rename it.

# .memory.md — documented-stub-registry
AVOID: a stub without a registry owner — tests/stub_registry.test.ts will fail the build.
AVOID: deleting the typed signature when stubbing; callers must still compile to the contract.
LESSON: owner is a STORY-* (deterministic, near-term) or ROADMAP:phase-* (LLM/net/git/IO, later).

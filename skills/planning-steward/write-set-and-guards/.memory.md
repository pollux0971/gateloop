# write-set-and-guards — memory

AVOID: repo-wide globs (`**`, `*`) — they make the Permission Gateway a no-op.
AVOID: forgetting the three guards (secret/sudo/api); validateForbiddenActions will reject the contract.
AVOID: overlapping write-sets across parallel_safe stories — that reintroduces clobbering.

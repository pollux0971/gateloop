# .memory.md — interface-contract-spec
AVOID: prose-only component descriptions — an LLM will invent the signature and drift.
AVOID: `any` on public APIs; AVOID caller-supplied trust flags (isDisposable) — inject an oracle.
LESSON: declare fs/network/git side-effects explicitly so they map to a permission decision.

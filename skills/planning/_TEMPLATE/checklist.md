# Completion checklist

The stage is `done` only when every item below passes (evaluated in PSKILL.3).
Write one checkable item per line as `- [ ] <condition>`.

- [ ] the Overview section is present and non-empty
- [ ] every required section from template.md is filled in
- [ ] there are no TBD / placeholder markers left

# Step 2 — Author the document

Fill in `template.md` section by section. Keep every claim testable; avoid TBDs.
When finished, run the completion check against `checklist.md` — every item must
pass for the stage to become `done`.

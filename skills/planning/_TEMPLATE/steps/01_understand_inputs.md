# Step 1 — Understand the inputs

Read the prior-stage artifacts declared in the frontmatter `inputs`. Confirm the
upstream stage is `done` before authoring (the workflow engine enforces order;
this step is the human-readable counterpart).

- What is the goal of this document?
- Which prior decisions constrain it?
- What is explicitly out of scope?

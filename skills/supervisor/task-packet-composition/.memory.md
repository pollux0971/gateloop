# task-packet-composition — memory
AVOID: widening `allowed_write_set` beyond the contract — the Permission Gateway will reject the packet.
AVOID: setting `target_agent` to an unknown role — routing will fail silently at dispatch time.
AVOID: omitting `attempt_budget` or setting it to zero — the receiving agent has no retry ceiling.

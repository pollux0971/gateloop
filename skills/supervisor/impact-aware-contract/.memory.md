# impact-aware-contract — memory
AVOID: issuing a contract without regression_tests — brownfield patches always need coverage of the impacted area.
AVOID: omitting rollback_ref — Developer must know the revert target before applying.
AVOID: empty impact_set; enumerate callers and dependents before issuing.

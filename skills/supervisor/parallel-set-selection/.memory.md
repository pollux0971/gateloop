# parallel-set-selection — memory
AVOID: including `sequential` stories in the parallel set — the harness will serialize them, breaking the assumption.
AVOID: ignoring glob-prefix overlap — two write-set globs sharing a common prefix can mutate the same files concurrently.
AVOID: referencing a story_id that is not in the candidate stories list — evaluate will reject it.

# decision-matrix-routing — memory
AVOID: emitting multiple actions in one routing call — the harness executes exactly one per turn.
AVOID: `route_debugger` after a validation pass — that discards already-passing work.
AVOID: any action other than `escalate_human` when the attempt budget is exhausted.

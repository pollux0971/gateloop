# budget-governance — memory
AVOID: `continue` verdict when `attempts_used >= attempt_budget` — infinite loops are a harness failure mode.
AVOID: ignoring `same_signature_count >= 2` — a repeated failure pattern means the fix strategy is wrong and must escalate.
AVOID: using `halt` when the root cause is unknown — prefer `escalate` so a human can intervene.

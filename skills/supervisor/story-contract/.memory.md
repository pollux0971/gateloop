# story-contract — memory
AVOID: prose acceptance in the contract; only files/behaviors/commands are enforceable.
AVOID: widening the write-set beyond the spec; widening needs human-approved revision.
AVOID: omitting the three forbidden-action guards — validateStoryContract will reject it.

# escalation-triage — memory
AVOID: auto-approving `needs_scope_expansion` — scope changes alter the contract and always require a human gate.
AVOID: `retry_with_guidance` on `repeated_failure` without specifying `retry_limit` — it will loop forever.
AVOID: empty `rationale` — the Supervisor needs a trace to explain the decision to downstream agents and auditors.

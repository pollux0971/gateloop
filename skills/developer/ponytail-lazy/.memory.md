# ponytail-lazy — compact lessons

AVOID: removing an existing exported function to "simplify" — the additive gate rejects it; simplify only your own additions or dead code this patch made redundant.
AVOID: silently dropping a contracted requirement to be lazy — build the minimum that satisfies it and raise the doubt as an escalation instead.

# pre-flight-check — memory
AVOID: unbounded self-correction (cap 2); repeated signature ⇒ escalate.
AVOID: running anything outside typecheck / affected tests; never mark a failed pre-flight as passed.

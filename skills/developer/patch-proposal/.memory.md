# patch-proposal — memory
AVOID: wholesale rewrites — prefer additive change_type (ADD/INSERT_PREREQ) when possible.
AVOID: changed_files outside the write-set — escalate (needs scope expansion), never widen.
AVOID: omitting rollback_notes — every proposal must be reversible.

# task-decomposition — memory

AVOID: a single "implement everything" subtask — small steps are the whole point.
AVOID: a subtask needing a file outside the contract write-set — escalate (needs scope expansion), never widen.
AVOID: dependency cycles or depending on a later subtask — keep the plan a forward DAG.

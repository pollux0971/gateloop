# root-cause-hypothesis-ranking — memory
AVOID: one-word or symptom-restatement hypotheses — they must be falsifiable.
AVOID: sorting hypotheses ascending — confidence must be descending (highest first).
AVOID: returning an empty hypotheses list — always include at least one.

# test-output-interpretation — memory
AVOID: inferring what the developer intended; only record what the test output proves.
AVOID: omitting evidence_lines — they are required for downstream hypothesis ranking.
AVOID: setting implementation_intent — this field must never appear in the output.

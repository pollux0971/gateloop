# ponytail-review — compact lessons

AVOID: recommending deletion of an existing exported function unless the contract permits it — the additive gate will reject such a patch; prefer "inline", "collapse to one caller", "drop the unused branch".
AVOID: flagging the single smoke test / assert self-check as bloat — it is the required ponytail minimum, not over-engineering.
